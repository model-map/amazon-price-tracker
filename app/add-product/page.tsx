import { SiteHeader } from "@/components/site-header";
import { Input } from "@/components/ui/input";

const page = () => {
  return (
    <div className="flex items-center justify-center h-[50vh] w-[50vw] mx-auto"></div>
  );
};

export default page;
